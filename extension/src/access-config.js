// Replaced inside the XPI by build:xpi -- --access-config <file.json>.
// No mailbox changes or sending are enabled by default.
globalThis.TB_ACCESS_CONFIG = Object.freeze({ downloadAttachments: true, compose: true });
